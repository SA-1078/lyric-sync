# LyricSync — Dependencias de Python
# Instalar con: pip install -r requirements.txt

openai-whisper    # Motor de transcripción IA (offline)
rapidfuzz         # Fuzzy matching para post-procesador de letras
